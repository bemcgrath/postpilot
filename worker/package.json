{
  "name": "postpilot-rewrite-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: metered proxy for PostPilot's AI Rewrite feature. Holds the Anthropic API key server-side, validates Pro entitlement against LemonSqueezy, and enforces per-user daily caps so PostPilot (not the user) pays for generations.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2",
    "wrangler": "^4.0.0"
  }
}
