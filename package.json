{
  "name": "postpilot",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "worker"
  ],
  "scripts": {
    "build:score": "node scripts/score-widget/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.18.20"
  }
}
