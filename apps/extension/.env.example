# Copy to `.env` in this directory. Plasmo inlines PLASMO_PUBLIC_* at build time.
# Generate: openssl rand -hex 32
# Then set the SAME value on the worker:
#   cd worker && wrangler secret put REWRITE_CLIENT_SECRET
PLASMO_PUBLIC_REWRITE_KEY=
